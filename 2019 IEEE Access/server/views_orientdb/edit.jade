doctype html
html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='') Visitor Log

        ul
            each topic in topics
                -rid=encodeURIComponent(topic['@rid'])
                li
                    a(href='/' + rid)=topic['title']
        articlednd
           -rid=encodeURIComponent(topic['@rid'])
            form(action='/'+rid+'/edit',method='post')
                p
                    input(type='text' name='title' placeholder='title' value=topic.title)
                p
                    input(type='textarea' name='description' placeholder='description' value=topic.description)
                p
                    input(type='text' name='author' placeholder='author' value=topic.author)
                p
                    input(type='submit')
